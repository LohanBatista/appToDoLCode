import React from 'react';

import {Container, LogoIcon, TwoPoint} from './styles';

export const Logo: React.FC = () => {
  return (
    <Container>
      <LogoIcon />
      <TwoPoint />
    </Container>
  );
};
